# 验证记录

## 验证环境与方法

验证日期：2026-09-04。使用 Windows、Python 3.11.1、NumPy 2.2.6、SciPy 1.16.3、PyYAML 6.0.3、ContourPy 1.3.2；规范检查使用 Ruff 0.16.6。

以下结果来自当前独立实现及自行构造的合成数据，没有使用外部项目实现作为正确性依据。真实地形与实测深度尚未用于本次验证。

```shell
python -X utf8 -m unittest discover -s tests -t .
ruff check .
python -X utf8 -m tests.evaluate_accuracy --output .codex_tmp/accuracy.json
```

21 项测试通过，Ruff 检查通过。测试覆盖独立标量插值、朴素穷举 MAD、连续线段投影、刚体变换、KF 解析更新和角度周期、PF 贝叶斯权重和重采样，以及完整轨迹的数据隔离、窗口裁剪、重复调用、无效区域和精度回归。

打包配置显式声明构建工具和 YAML 包数据。按用户要求，停止额外的库及打包验证；不将 wheel 构建、安装或独立目录导入记为已验证。

测试代码所用主要地形种子为 101，下面的精度验收地形种子为 20260904，观测噪声种子为 20260905。初始参数为预先指定的工程验证配置，没有对下表场景进行参数搜索。

## 合成场景与配置

地图尺寸为 144×144，轨迹长 80，窗口长 32，各方法返回并评估前 49 点。地图包含多个非对称地形成分，深度由独立标量双线性公式生成。

| 场景 | 注入误差 |
|---|---|
| 整数平移 | INS 平移 `[3, -2]` 像素 |
| 亚像素平移 | INS 平移 `[2.75, -1.25]` 像素 |
| 航向误差 | 亚像素平移加 2° 刚体旋转误差 |
| 漂移、低噪声 | 亚像素平移加全程线性累计 `[2, -1]` 像素漂移，深度噪声标准差 0.25 |
| 漂移、高噪声 | 同一漂移轨迹与同一标准正态噪声实现，深度噪声标准差扩大到 1 |

评估覆盖参数如下，其余使用库默认值：

- 窗口长度 32；TERCOM 粗搜索半径 8、间距 1；ICCP 初始化半径 8、间距 1。
- PF 初始化半径 8、过程标准差 0.2；观测标准差取 `max(0.2, 合成噪声标准差)`；种子分别为 0、1、2、3、4。
- TERCOM、ICCP 各自运行关闭及开启 KF 两种模式。

这些是具有已知噪声尺度和有限位置先验的合成实验配置，与发布默认配置中的半径 32 等数值有区别。算法内部始终不使用参考轨迹位置。

## 位置 RMSE

单位为像素；PF 单元格列出五个种子的中位数及最小、最大值。数值四舍五入到四位小数。

| 场景 | INS | TERCOM | TERCOM + KF | ICCP | ICCP + KF | PF 中位数（范围） |
|---|---:|---:|---:|---:|---:|---:|
| 整数平移 | 3.6056 | 0.0000 | 0.0035 | 0.0276 | 0.0320 | 0.4232（0.4086～0.4558） |
| 亚像素平移 | 3.0208 | 0.0000 | 0.0029 | 0.0276 | 0.0320 | 0.3928（0.3620～0.4691） |
| 航向误差 | 3.4308 | 0.4052 | 0.3414 | 0.0276 | 0.0320 | 0.3480（0.3268～0.4010） |
| 漂移、低噪声 | 3.7213 | 0.5070 | 0.4349 | 1.2967 | 0.9167 | 0.4402（0.4197～0.4723） |
| 漂移、高噪声 | 3.7213 | 0.7647 | 0.7303 | 6.1276 | 1.2512 | 0.6498（0.6289～0.6555） |

航向误差场景中，ICCP 最终旋转角误差为约 0.0024°；开启 KF 后约为 0.0260°。

TERCOM 的零误差仅表示该合成场景中的真实平移恰好位于精搜索网格上，且无观测噪声；不代表任意平移、真实地图或噪声条件下的保证。ICCP 的小幅残差也包含等深线线段近似与双线性地形等值曲线之间的差异。

## 精度限制与解读

**高噪声且窗口内持续漂移时，未开启 KF 的 ICCP 在本实验中比原始 INS 更差。** 经典最近等深线配准可能在错误对应关系下获得局部解，二维刚体模型也无法精确表达窗口内变化的漂移。本结果不应被解释为每种基线在所有场景中都能改善定位。

在此场景中，开启 KF 后 ICCP RMSE 从 6.1276 降至 1.2512 像素。但无噪声场景里 KF 会引入少量平滑偏差，因此保留开关及对照结果，并不默认认定滤波更准确。

这里的 PF 使用已知噪声尺度，并在有限初始位置范围内采样。真实实验中应另外检查初始先验是否覆盖真实位置、观测噪声模型及多峰分布下的粒子退化；标准逐点 PF 的加权均值也不保证位于某个后验模式上。

TERCOM、ICCP 对每个输出点使用前向窗口，PF 每步仅使用当前及已积累的观测。输出区间虽一致，观测信息量不同，因此此表用于核验实现和展示场景响应，不作为三种方法的普遍排名。

完整 JSON 报告还包括平均误差、95%分位误差、最大误差、逐次运行时间、PF 每个种子结果和配置覆盖，可通过上述命令重新生成。增加 `--trace-memory` 可记录 tracemalloc 分配峰值，该值不是操作系统进程驻留内存。未设置速度或内存优化倍数门槛。
